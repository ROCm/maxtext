FlyDSL + MaxText MoE — Technical Overview
==========================================

Working document for the FlyDSL MoE integration in MaxText: JAX bridge, kernel
design, configs, lowering, correctness, and performance on MI355X (gfx950, ROCm).

Shorter validation summary: see flydsl_moe_validation.md in the same docs folder.


0. THE TWO LAYERS
-----------------

1. FlyDSL — MLIR-native kernel DSL for AMD GPUs (gfx950 / gfx942). Python kernels
   compile to MLIR, then ROCDL, then HSACO binaries using matrix cores (MFMA).
   Pinned in flydsl_pin.txt; imported as kernels.* and flydsl.*.

2. jax-flydsl (jax_flydsl/) — bridge that makes a FlyDSL @flyc.jit kernel callable
   from JAX as a primitive. No PyTorch dependency.

On top: maxtext_moe/ swaps MaxText's routed-MoE layer for FlyDSL grouped GEMM.


1. JAX-FLYDSL: THE JAX ↔ FLYDSL BRIDGE
--------------------------------------

Files:

  flydsl_lib.py     — flydsl_call primitive: eager impl + jit lowering, MLIR compile cache
  adapter.py        — JaxTensorAdaptor: MLIR types from jax.Array via DLPack
  bridge.py         — compiles C trampoline, registers XLA custom-call target, slot table
  _flydsl_bridge.c  — lock-free C trampoline on XLA's dispatch thread


1.1 Type inference — adapter.py

FlyDSL's MLIR emission needs shape/dtype/layout per argument. adapter.py registers
JaxTensorAdaptor for jax.Array (replacing the default torch adaptor):

  - DLPack export (array.__dlpack__(stream=0)) for GPU pointer + metadata
  - FP8 dtypes viewed as uint8 for DLPack (bit pattern preserved)


1.2 The primitive — flydsl_lib.py

Entry point:

  flydsl_call(*args, kernel, out_shape, scalars=None,
              output_positions=None, input_output_aliases=None)

Parameters:

  *args                  jax.Array GPU inputs, in launcher buffer-param order
  kernel                 @flyc.jit launcher (e.g. build_layernorm_module(...))
  out_shape              ShapeDtypeStruct or list — output shape/dtype spec(s)
  scalars                dict[str, int] — compile-time ints, keyed by param name
  output_positions       list[int] — launcher buffer slots that are outputs
                         (default: outputs come last)
  input_output_aliases   dict[int, int] — {input_idx: output_idx} for in-place

Steps:

  1. Launcher introspection (_inspect_launcher_layout): read @flyc.jit signature;
     classify each parameter → buffer (fx.Tensor), scalar (fx.Int32), or stream
     (fx.Stream).

  2. _abstract_eval: declare output ShapedArray(s) (shape/dtype only; no compute).

  3. flydsl_call_p.bind(...) — enter JAX primitive.

  4a. Eager mode (outside jax.jit):
      def_impl → xla.apply_primitive(flydsl_call_p) — compile and run a one-op
      XLA program for this call.

  4b. JIT mode (inside jax.jit):
      mlir.register_lowering(flydsl_call_p, _lowering, platform="rocm")
      → see section 1.3.


1.3 _lowering (jit path)

1. Build slot map: scalar vs buffer per launcher param; buf_idx[] maps XLA buffers
   to FlyDSL slots (output_positions can place output at arbitrary slot).

2. build_module(kernel, *dummies, **scalars) produces the MLIR module (GPU module + ciface).

3. Compile cache: cache_key = sha256(mlir_asm). On miss: MlirCompiler.compile produces
   HSACO + host func ptr; register_kernel stores in C slot table and returns slot_idx.

4. Emit jax.ffi.ffi_lowering XLA custom call with backend_config=slot_idx and
   operand_output_aliases.

Note: Compile once per unique (shape, tile, activation, dtype) — handful per model.


1.4 The C trampoline — bridge.py + _flydsl_bridge.c

  - Auto-compiles _flydsl_bridge.c to _flydsl_bridge.so on first import.

  - Registers flydsl_bridge_dispatch as XLA custom-call target.

  - Slot table: g_slots[MAX_SLOTS=1024], lock-free dispatch (registration mutex only).

  - Runtime: XLA calls flydsl_bridge_dispatch(stream, buffers, opaque), repacks
    buffers + scalars + stream into void** packed, then calls HSACO func(packed).


1.5 Compilation path (diagram)

  [INSERT IMAGE: docs/flydsl_bridge_diagram.png]
  Caption: JAX ↔ FlyDSL bridge — eager vs jit, MLIR compile cache, C trampoline → HSACO.

Scope: inference-only (no jvp/transpose — forward prefill/decode, not training).


2. WHAT MAKES FLYDSL GOOD FOR MOE
---------------------------------

  - MLIR-native MFMA kernels — direct matrix-core intrinsics, LDS control.

  - Fills ROCm stack gaps — bf16/fp8 grouped-GEMM MoE on gfx950 where stock ragged_dot
    has no grouped-GEMM lowering (see section 10).

  - Epilogue fusion — SwiGLU/GeGLU in stage-1; weighted combine in stage-2.

  - Pre-shuffled weights — offline MFMA tile layout; no runtime swizzle.

  - Software-pipelined K-loop — double-buffered LDS, XOR16 swizzle.

Also: layernorm, rmsnorm, softmax, preshuffle GEMM, flash attention, etc.


3. REPO LAYOUT (ESSENTIALS)
---------------------------

  jax-flydsl/
    jax_flydsl/      JAX ↔ FlyDSL bridge
    maxtext_moe/     MaxText MoE integration
    benchmarks/      kernel + MoE microbenchmarks
    tests/           kernel + MoE correctness
    scripts/         setup + MaxText run scripts
    docs/            this overview + validation + diagrams
    results/         compare_backends / decode reports
    flydsl_pin.txt


4. JAX-FLYDSL BENCHMARKS — MI355X (GFX950, ROCM 7.2)
------------------------------------------------------

All wall-clock, median of 50 iters. Speedup = pure JAX / jax-flydsl (>1 = FlyDSL faster).
Single GPU. Setup: AMD MI355X (gfx950), ROCm 7.2, 8 TB/s HBM3E.


4.0 How to rerun

Environment (inside flydsl-dev container):

  source /workspace/.flydsl_env
  export HIP_VISIBLE_DEVICES=2          # pin to one GPU (or FLYDSL_BENCH_DEVICE=N)

Harness (benchmarks/bench_utils.py — shared by all bench_*.py):

  - jax.config jax_default_device pinned to one GPU
  - bench_one(fn, warmup, iters, flush): warmup iters, then median wall-clock of
    iters timed runs with fn(...).block_until_ready()
  - GC disabled during measurement
  - Norm benches: flush_l2() before each timed iter (cold-cache, bandwidth-realistic)
  - GEMM bench: flush=False (compute-bound; matches hipBLASLt comparison style)

Canonical flags for the tables below: --warmup 5 --iters 50

Run full sweeps (stdout has every shape; tables report largest per dtype only):

  # Norms — built-in shape lists in each script (f32 / f16 / bf16)
  python3 -m benchmarks.bench_softmax   --warmup 5 --iters 50
  python3 -m benchmarks.bench_layernorm   --warmup 5 --iters 50
  python3 -m benchmarks.bench_rmsnorm     --warmup 5 --iters 50

  # Dense FP8 GEMM — M=16..5120 vs jnp.matmul (XLA→hipBLASLt)
  python3 -m benchmarks.bench_preshuffle_gemm --warmup 5 --iters 50

  # MoE — Mixtral E=8 topk=2, tokens 1/8/32/256/1024/2048/4096 (FP8, 2-stage fly)
  python3 -m benchmarks.bench_moe           --all --warmup 5 --iters 50
  python3 -m benchmarks.bench_moe_baseline  --all --warmup 5 --iters 50

  # aiter-CK (torch subprocess; FlyDSL checkout on PYTHONPATH)
  PYTHONPATH=/workspace/FlyDSL/python:/workspace/FlyDSL python3 benchmarks/aiter/bench_aiter_moe.py \
      --tokens 64 --experts 8 --topk 2 --model_dim 4096 --inter_dim 14336 \
      --warmup 5 --iters 50
  # repeat --tokens for 256 1024 2048 4096, or use --all with Mixtral presets

How tables are curated from sweep output:

  - Do NOT average over all shapes — small M/N hit ~45–55 µs JAX dispatch floor and
    drag averages down.
  - Norm tables: largest shapes per dtype (kernel-bound regime). f32/f16: top ~3 rows
    from each dtype block; bf16: top ~6 rows where FlyDSL wins are visible.
  - GEMM table: subset of M (16, 64, 128, 1024, 2048, 4096, 5120) from full sweep.
  - MoE table: tokens 64, 256, 1024, 2048, 4096 — merge fly + baseline + aiter runs.
  - speedup column = pure_JAX_us / jax_flydsl_us (same formula in every script).


4.1 Norm kernels — jax-flydsl vs jax.nn.* (XLA Triton emitter)


Softmax — all dtypes, largest shapes per dtype

  dtype  shape               jax-flydsl (µs)  pure JAX (µs)  speedup
  f32    (1024, 4096)        66.7             71.7           1.08x
  f32    (2048, 2048)        66.0             69.7           1.05x
  f32    (4096, 8192)        114.2            113.1          1.00x
  f16    (8192, 2048)        75.0             82.6           1.10x
  f16    (16384, 2048)       89.5             95.6           1.07x
  f16    (32768, 8192)       241.0            245.6          1.02x
  bf16   (16384, 2048)       77.8             88.2           1.13x
  bf16   (16384, 8192)       144.2            184.9          1.28x
  bf16   (32768, 8192)       241.6            310.0          1.28x
  bf16   (65536, 8192)       429.9            561.7          1.31x
  bf16   (32768, 16384)      449.0            551.2          1.23x
  bf16   (131072, 2048)      239.9            314.8          1.31x


LayerNorm — all dtypes, largest shapes per dtype

  dtype  shape               jax-flydsl (µs)  pure JAX (µs)  speedup
  f32    (2048, 4096)        61.5             87.4           1.42x
  f32    (4096, 8192)        190.3            187.2          0.98x
  f32    (32768, 8192)       547.6            557.7          1.02x
  f16    (8192, 2048)        138.0            113.9          0.83x
  f16    (16384, 2048)       134.2            136.9          1.02x
  f16    (32768, 8192)       309.8            282.8          0.91x
  bf16   (4096, 2048)        136.7            166.3          1.22x
  bf16   (8192, 2048)        104.3            160.5          1.54x
  bf16   (32768, 8192)       299.6            307.8          1.03x
  bf16   (65536, 8192)       518.0            498.5          0.96x
  bf16   (32768, 16384)      509.1            518.8          1.02x
  bf16   (131072, 2048)      330.3            324.3          0.98x


RMSNorm — all dtypes, largest shapes per dtype

  dtype  shape               jax-flydsl (µs)  pure JAX (µs)  speedup
  f32    (2048, 2048)        53.9             74.8           1.39x
  f32    (16384, 8192)       270.4            279.9          1.04x
  f32    (32768, 8192)       470.0            487.6          1.04x
  f16    (4096, 2048)        92.0             103.1          1.12x
  f16    (16384, 2048)       109.0            107.3          0.98x
  f16    (32768, 8192)       263.8            272.0          1.03x
  bf16   (4096, 2048)        88.2             116.7          1.32x
  bf16   (8192, 2048)        84.1             97.4           1.16x
  bf16   (16384, 8192)       159.4            178.3          1.12x
  bf16   (32768, 8192)       275.3            295.7          1.07x
  bf16   (65536, 8192)       452.4            501.1          1.11x
  bf16   (32768, 16384)      454.2            486.0          1.07x
  bf16   (131072, 2048)      257.5            294.7          1.14x


Norm cross-dtype takeaway:

  - f32 / f16: essentially tied
  - bf16 softmax + RMSNorm: 1.07–1.31x wins at large shapes. XLA's bf16 Triton emitter
    inserts redundant bf16↔f32 conversions in the inner loop (4 for softmax, 2 for RMSNorm).
  - bf16 LayerNorm: ties at large shapes because the standard JAX LayerNorm reference
    has 0 redundant bf16↔f32 casts

The number of redundant bf16↔f32 conversions in the JAX reference linearly predicts
FlyDSL's advantage: 0 → tied (LayerNorm), 2 → 1.07–1.14x (RMSNorm), 4 → 1.22–1.31x
(Softmax).


4.2 GEMM — jax-flydsl preshuffle FP8 vs jnp.matmul (XLA → hipBLASLt FP8)

  M      jax-flydsl (µs / TFLOPS)   pure JAX (µs / TFLOPS)   speedup
  16     69.0 / 19.4                68.9 / 19.5              1.00x
  64     65.6 / 81.8                63.8 / 84.1              0.97x
  128    67.2 / 159.9               65.2 / 164.6             0.97x
  1024   109.3 / 785.8              93.8 / 915.7             0.86x
  2048   135.5 / 1267.9             110.6 / 1552.6           0.82x
  4096   201.8 / 1702.4             181.4 / 1893.8           0.90x
  5120   257.4 / 1694.5             223.6 / 1950.9           0.87x

GEMM takeaway: hipBLASLt has more speedup in plain A·B at large matrix sizes. For
small/decode shapes they're tied. For fused A·B + C (bias/residual/activation), FlyDSL
has epilogue fusion support.


4.3 MoE GEMM — jax-flydsl vs pure JAX (hipBLASLt FP8 batched) vs aiter-CK torch

E=8, topk=2, model=4096, inter=14336. FlyDSL 2-stage (stage2=atomic).

  tokens  pure JAX (µs)  jax-flydsl (µs)  speedup vs pure JAX  aiter-CK (µs)  aiter / FlyDSL
  64      170.0          152.7            1.11x                63.1           aiter 2.42x
  256     593.9          451.4            1.32x                369.3          aiter 1.22x
  1024    1556.9         882.0            1.77x                836.6          tied (1.05x)
  2048    3121.1         1427.3           2.19x                1480.8         FlyDSL 1.04x
  4096    5927.1         2578.4           2.30x                2800.8         FlyDSL 1.09x

MoE takeaway: torch+aiter performs better at small token counts (aiter 1.2–2.4x faster,
single fused kernel) but jax-flydsl performs better at ≥2048 tokens (FlyDSL 1.04–1.09x
faster than aiter).


4.4 Comparison caveats

  - MoE/GEMM benches are fp8; end-to-end model numbers are bf16.

  - Norm wins are bf16-specific (XLA cast artifact); f32/f16 approximately tied.

  - aiter fused single-kernel path is faster at tiny token counts; 2-stage table is
    apples-to-apples.

  - dense MoE drops tokens at capacity — not equivalent to ragged/fly.

  - ragged baseline on our stack (JAX 0.8.2 / bf16 / MI355) is NOT grouped GEMM
    (see section 10).


5. MAXTEXT INTEGRATION
----------------------

Non-invasive overlay — no MaxText source edits.

Usage pattern:

  from maxtext_moe.backend import fly_moe_backend, load_params_flydsl

  with fly_moe_backend("gemma"):
      engine = maxengine.MaxEngine(config)
      params = load_params_flydsl(engine, rng, model="gemma")
      # prefill / decode ...

Steps:

  1. fly_moe_backend(model) — swaps RoutedMoE to FlyRoutedMoE for model build.

  2. load_params_flydsl — stock load + one-time preshuffle conversion
     (fly_w1/w2_shuffled, drop wi_0/wi_1/wo).

  [INSERT IMAGE: docs/flydsl_diagram_2_integration.png]
  Caption: Layer swap → weight conversion → runtime _fly_call.


6. FLYDSL MOE FORWARD PATH
--------------------------

  1. Router (_route) — mirrors MaxText get_topk
     Gemma: softmax-all then gather
     Mixtral: top-k then softmax

  2. moe_sort_jax — replicate k×, sort by expert.

  3. Stage 1 — moe_gemm1 grouped GEMM + fused activation.

  4. Stage 2 — moe_gemm2 (atomic or reduce) down-proj + weighted combine.

  [INSERT IMAGE: docs/flydsl_diagram_3_twostage.png]
  Caption: Sort → stage-1 → stage-2 over exactly routed tokens.

Preshuffle: fuse gate+up, pad inter_dim to 128, shuffle to MFMA layout (common/preshuffle.py).


7. MODELS
---------

                          Mixtral-8x7B          Gemma4-26B
  experts / topk          8 / 2                 128 / 8
  moe_mlp_dim             14336                 704 (768 padded)
  emb_dim                 4096                  2816
  activation              silu                  gelu
  router                  top-k then softmax    softmax-all then gather
  shared expert           none                  yes


8. CONFIGS (MAXTEXT BASELINE VS FLY)
------------------------------------

Baseline ragged_dot path:

  sparse_matmul=True
  megablox=False
  use_tokamax_gmm=False
  dtype=bfloat16
  attention=dot_product          (gfx950: not TPU RPA)
  scan_layers=false

Fly is selected by fly_moe_backend() context manager, not a yaml flag.
FLY_STAGE2=atomic or reduce picks stage-2 reduction.

For hipBLASLt grouped-GEMM lowering (NOT available on our stack):
  JAX >= 0.9.2
  xla_gpu_experimental_use_ragged_dot_grouped_gemm=true
  shardy=true
  fp16 on MI355 (bf16 grouped GEMM only on MI300)


9. END-TO-END PERFORMANCE (BF16 PREFILL, MI355X)
------------------------------------------------

Baseline = stock MaxText ragged_dot. Single device, batch=1.

Mixtral:

  length    ragged (ms)    fly (ms)    speedup
  256       72.6           29.1        2.50×
  1024      174.9          61.3        2.85×
  2048      288.3          115.8       2.49×

Decode 1 token: 69.8 ms → 19.7 ms = 3.54× speedup.

Gemma (pretrained weights):

  length    ragged (ms)    fly (ms)    speedup
  256       261.2          32.6        8.0×
  1024      645.2          29.6        21.8×
  2048      1013.5         45.8        22.1×

Per-layer at Mixtral seq=1024: MoE GEMMs 151.7 ms (ragged) → 42.6 ms (fly) ≈ 3.6× on MoE alone.


10. WHAT RAGGED_DOT ACTUALLY CALLS (GFX950 / JAX 0.8.2 / BF16)
---------------------------------------------------------------

  [INSERT IMAGE: docs/flydsl_diagram_4_lowering.png]
  Caption: ragged_dot (masked batched E×) vs FlyDSL grouped GEMM.

Algorithmically: dropless, exact — only routed token–expert pairs count in the result.

On our stack (kernel level): NOT grouped GEMM. Lowers to:

  broadcast x[T,K] → [E,T,K]; mask by group_sizes
  Triton batched dot [E,T,K] × [E,K,N] → [E,T,N]   (__triton_nested_gemm_fusion)
  reduce over E → [T,N]

Every token is computed against every expert slot, then masked — E× FLOPs.

MaxText prefill HLO example (Mixtral, bf16):

  gemm_fusion_dot: bf16[1024,4,28672] × bf16[4,28672,4096] → f32[4,1024,4096]
                   kind=__triton_nested_gemm_fusion

Zero hipBLASLt grouped-GEMM MoE calls in the module. Attention QKVO uses rocBLAS GEMM.

True grouped GEMM (hipBLASLt, JAX >= 0.9.2): fp16-only on MI355; bf16 on MI300.
FlyDSL implements grouped GEMM directly via XLA custom call → HSACO.


11. CORRECTNESS
---------------

See flydsl_moe_validation.md for full methodology. Three regimes matter:

  1. Prefill — last-position logits on real weights (primary MoE kernel check)
  2. Decode-step-1 — first generate() logits with identical KV state (isolates
     single-token MoE from free-running degeneration)
  3. Free-running greedy — gen_steps=8 via real KV-cache generate() path


11.1 Prefill (primary signal)

Tool: compare_backends.py — 10 prompts, real weights, last-position logits.

  model      top-1 fly vs ragged    cosine
  Mixtral    10/10                  >= 0.99998
  Gemma      10/10                  >= 0.99988

Reports: results/compare_mixtral_real/report.md
         results/compare_gemma_real/report.md


11.2 Decode — methodology

Tool: compare_decode.py
  seq=512, gen_steps=8, greedy, dtype=bfloat16, single device, real weights
  Uses MaxEngine prefill → insert → generate() (actual KV-cache path, not teacher forcing)

Two metrics (do not conflate):

  full sequences exact — entire 8-token greedy output identical fly vs ragged?
  decode-step-1 top-1 — after prefill, same argmax on first generate() logit vector?
                        (cosine ~1.0, top-5 overlap 100% when Y)

Mixtral passing BOTH metrics proves fly MoE is exact on the decode-path kernel.
Gemma failing BOTH backends on sequences while sometimes agreeing step-1 proves a
MaxText decode bug, not a fly regression.


11.3 Mixtral decode — 10/10 exact (ragged == fly)

Report: results/decode_mixtral/report.md

Decode-step-1: 10/10 top-1, cosine >= 0.99989, top-5 overlap 100% on all prompts.

Free-running greedy (8 tokens each) — fly text == ragged text on all 10:

  prompt                              generated continuation (both backends)
  "The capital of France is"          "a city that is known for its beauty"
  "In a galaxy far, far away,"        "a long time ago, a young man"
  "def fibonacci(n):"                 "\n    if n == 0:"
  "Photosynthesis is the process..."  "plants convert light energy into chemical energy."
  "The quick brown fox jumps..."      "lazy dog.\n\nThe quick brown"
  "Once upon a time, there was a"     "little girl who loved to read. She"
  "The first president of the US..."  "George Washington. He was born on February"
  "Water boils at a temperature of"   "100 degrees Celsius"
  "The largest planet in our..."      "Jupiter. It is the fifth planet"
  "import numpy as np"                "\nimport matplotlib.pyplot"

End-to-end decode latency (1 token, same harness as prefill bench):
  ragged 69.8 ms → fly 19.7 ms = 3.54× speedup


11.4 Gemma decode — NOT a fly defect

Base ckpt (gemma4_ckpt): results/decode_gemma/report.md (on disk in run env)
  full sequences exact: 0/10 (both backends)
  decode-step-1 top-1:  5/10 (both backends)

Instruction-tuned ckpt (-it): results/decode_gemma_it/report.md
  full sequences exact: 6/10 fly vs ragged (both produce same gibberish on those 6)
  decode-step-1 top-1:  6/10

Key observation: ragged (stock MaxText) and fly fail identically. First token from
prefill is often sensible; degeneration starts at the first KV-cache generate() step.

Example (-it, both backends match on degenerate runs):
  "The capital of France is" → ragged: " DO�© \n\n\n©<end_of_turn>"
                               fly:    " CAP��1️⃣1️⃣1"
  "In a galaxy far, far away," → both: " own's's's'"


11.5 Re-prefill diagnostic (localizes the bug)

For prompt + token₁ from prefill, compare:
  A) re-prefill(prompt + token₁) → logits for token₂   (no KV cache)
  B) generate() after prefill → logits for token₂      (KV cache)

Base Gemma, ragged backend:

  prompt                         token₁      re-prefill token₂   decode token₂   cosine(logits)
  "The capital of France is"     " a"        " city" ✓           garbage ✗       ~−0.10
  "The largest planet... is"     " Jupiter"  "." ✓               garbage ✗       0.34
  "The first president... was"   " George"   " Washington" ✓     garbage ✗       ~−0.13

Re-prefill gives coherent continuations → weights and prefill MoE path are correct.
Decode logits are uncorrelated with correct logits → KV-cache propagation broken in
MaxText Gemma-4 scanned blocks on this stack (upstream PR #4066 fixes scanned-layer
KV propagation for TPU vLLM-RPA; our ROCm dot_product decode path is unaffected).

Mixtral: re-prefill and decode agree (hence 10/10 sequences).


11.6 What we claim vs do not claim

  CLAIM:    fly MoE kernel is numerically exact vs ragged on prefill (both models)
            and on Mixtral decode (10/10 sequences + step-1 logits).
  CLAIM:    fly decode perf 3.54× on Mixtral single-token step.
  DO NOT:   claim Gemma end-to-end decode works on MaxText ROCm today.
  DO NOT:   attribute Gemma decode gibberish to fly — ragged matches fly byte-for-byte
            on degenerate sequences.


12. COMMANDS
------------

  source /workspace/.flydsl_env

  Prefill correctness:
  python3 -m maxtext_moe.compare_backends --model gemma --backends ragged,fly \
    --load_parameters_path results/gemma4_ckpt/0/items

  Decode correctness:
  python3 -m maxtext_moe.compare_decode --model mixtral --backends ragged,fly \
    --load_parameters_path results/mixtral_ckpt/0/items --gen_steps 8

  End-to-end perf:
  BACKEND=fly bash scripts/run_gemma_maxtext.sh


13. FUTURE WORK
---------------

  - Golden-logits check vs HF reference (forward_pass_logit_checker)
  - Multi-device sharding
  - fp8 end-to-end in MaxText
  - Fix or upstream Gemma KV-cache decode on ROCm maxengine path


APPENDIX — BUILDING CONFIDENCE
------------------------------

  1. Golden-logits vs HF (fp32, max_kl_div=0.03)
  2. More prompts + seeds in compare_backends
  3. fp32 reference (--dtype float32) — cosine 1.0 at precision floor
  4. Layer-level unit tests (tests/test_*_correctness.py)
  5. Per-stage intermediate agreement (stage-1 / stage-2)
  6. Roofline cross-check vs MoE microbench TFLOPs


DIAGRAM FILES (attach in Confluence)
------------------------------------

  docs/flydsl_bridge_diagram.png         — section 1.5 (JAX bridge)
  docs/flydsl_diagram_2_integration.png  — section 5 (MaxText integration)
  docs/flydsl_diagram_3_twostage.png     — section 6 (MoE dataflow)
  docs/flydsl_diagram_4_lowering.png     — section 10 (ragged vs fly lowering)
